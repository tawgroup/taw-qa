When writing a Playwright spec for a Sutagrow PR, follow the `qa-loop` skill (`.claude/skills/qa-loop/SKILL.md`). Snapshot the live DOM with Playwright MCP before writing `tmp/taw-qa.spec.mjs`.

Feature 2 (online) đi nhánh `online` của cùng skill đó: spec ghi vào `<checkout>/tests/e2e/taw-qa-pr-<n>.spec.ts`, không phải `tmp/`. Spec online: `docs/spec/feature-2.md`.
